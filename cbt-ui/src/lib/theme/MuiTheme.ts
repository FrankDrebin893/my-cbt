import {
	createMuiTheme
} from '@material-ui/core/styles';

const CustomMuiTheme = createMuiTheme({
	palette: {
		primary: {
			main: '#0f74d9',
		},
		secondary: {
			main: '#ffffff',
		},
	},
});

export default CustomMuiTheme;
